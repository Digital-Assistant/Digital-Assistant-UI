/**
 * events
 *
 * Re-exports the core SDK's event utilities (on, off, once, trigger)
 * from @digital-assistant/core so no duplicate implementation is needed.
 */
export { on, off, once, trigger } from "@digital-assistant/core";
